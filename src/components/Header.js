import Navigation from './Navigation';

function Header () {
    return (
        <header>
            <h1>Armando Rodriguez</h1>
            <Navigation />
        </header>
    );
}

export default Header;